% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_variance_estimate.R
\name{calculate_variance_estimate}
\alias{calculate_variance_estimate}
\title{Calculate Variance Estimate for Different Measures}
\usage{
calculate_variance_estimate(mu, vr, estimate = c("logOR", "RD", "logRR"))
}
\arguments{
\item{mu}{A numeric vector of probabilities (e.g., \code{mu_hat} values) for each dose level.}

\item{vr}{A variance-covariance matrix.}

\item{estimate}{A character string specifying the type of measure.
Options are "logOR" (log Odds Ratio), "RD" (Risk Difference), and "logRR" (log Risk Ratio).
Default is "logOR".}
}
\value{
A matrix representing the variance estimate for the chosen measure.
}
\description{
This function calculates the variance estimate for different measures (log Odds Ratio, Risk Difference, or log Risk Ratio)
based on the provided \code{mu} vector and the variance-covariance matrix \code{vr}.
}
\examples{
# Example usage:
muhat <- c(0.2, 0.5, 0.8)
vrhat <- diag(3)  # Example variance-covariance matrix
calculate_variance_estimate(mu = muhat, vr = vrhat, estimate = "logOR")
}
