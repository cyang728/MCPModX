% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_gc_variance.R
\name{calculate_gc_variance}
\alias{calculate_gc_variance}
\title{Calculate Variance of Model-Assisted Estimates for Binary Outcomes}
\usage{
calculate_gc_variance(data, glm_obj)
}
\arguments{
\item{data}{A data frame containing the variables \code{dose} and \code{y}.}

\item{glm_obj}{A fitted GLM object, typically created by the \code{glm} function.
Default is \code{anovaMod}.}
}
\value{
A variance-covariance matrix.
}
\description{
This function computes the variance of model-assisted estimates for binary outcomes.
The input data must contain columns \code{dose} and \code{y} (the binary outcome).
}
\examples{
# Example usage:
# Assuming `data` is your dataset and `anovaMod` is a fitted glm object.
# S <- calculate_gc_variance(data, anovaMod)
}
